name: Deploy

on:
  push:
    branches: [main]
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    # Only deploy if the CI workflow succeeded
    if: ${{ github.event_name == 'push' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy placeholder
        run: echo "Deploy step — configure your hosting provider (Railway, Fly.io, Render, VPS, etc.)"

      # Docker-based deployment example:
      # 1. Build the Docker image:
      #    - name: Build Docker image
      #      run: docker build -t unsaid:latest .
      #
      # 2. Push to a container registry (Docker Hub, GHCR, etc.):
      #    - name: Log in to GHCR
      #      uses: docker/login-action@v3
      #      with:
      #        registry: ghcr.io
      #        username: ${{ github.actor }}
      #        password: ${{ secrets.GITHUB_TOKEN }}
      #    - name: Push image
      #      run: |
      #        docker tag unsaid:latest ghcr.io/${{ github.repository }}/unsaid:latest
      #        docker push ghcr.io/${{ github.repository }}/unsaid:latest
      #
      # 3. Deploy to your hosting provider (Railway, Fly.io, Render, VPS via SSH, etc.)
      #    - Consult your provider's docs for the appropriate action or CLI command.